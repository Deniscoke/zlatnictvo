# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Vercel local state
.vercel/

# Claude Code personal settings (auto-push hook etc.)
.claude/settings.local.json

# Logs
*.log
npm-debug.log*

# Node
node_modules/
package-lock.json

# Local env
.env
.env.local
.env.*.local

# Build artifacts — regenerated by Vercel build (`npm run build`).
# Kept out of git so dev/prod CSS never drifts; Vercel rebuilds on every deploy.
tailwind.dist.css

# Image-pipeline metadata — regenerated by `npm run optimize:images`.
images/manifest.json
